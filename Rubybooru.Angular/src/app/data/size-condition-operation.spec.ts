import { SizeConditionOperation } from './size-condition-operation';

describe('SizeConditionOperation', () => {
  it('should create an instance', () => {
    expect(new SizeConditionOperation()).toBeTruthy();
  });
});
